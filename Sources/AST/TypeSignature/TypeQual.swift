/// A type qualifier.
public enum TypeQual {

  /// A type identifying an immutable assumption.
  case const

  /// A type identifying an assumption that is not bound to any scope.
  case unscoped

}
