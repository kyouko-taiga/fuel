func f(x) : \A a . ((!a + [a: Int]) -> Int + [a: Int])

func hello(x) : (Int) -> Int {
  local = salloc Int

  if true {
    store x, local
  } else {
    store 0, local
    foo = call f, local
  }

  y = load local
  return y
}
